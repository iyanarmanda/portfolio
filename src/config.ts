import type { SiteConfig, DeployConfig } from "./types/config";

export const siteConfig: SiteConfig = {
	title: "Ian Armanda | Software  Developer",
  description: "Software & Fullstack Developer with Expert on JavaScript Backend Developer",
};

// Deploy configuration (Netlify, GitHub Pages, Coludflared, etc)
export const deployConfig: DeployConfig = {
	siteUrl: "https://portfolio.izuli.my.id",
	baseUrl: "/",
};
