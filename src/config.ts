import type { SiteConfig, DeployConfig } from "./types/config";

export const siteConfig: SiteConfig = {
	title: "Iyan Z Armanda | Full-Stack Software Engineer",
  description: "I Specialize in backend architecture using Node.js and TypeScript, with a strong passion for building secure, system-level utilities using Rust and C++",
};

export const deployConfig: DeployConfig = {
	siteUrl: "https://portfolio.izuli.my.id",
	baseUrl: "/",
};
